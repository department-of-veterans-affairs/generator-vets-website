# <%= appName %>

## URL
http://localhost:3001<%= rootUrl %>
https://staging.va.gov<%= rootUrl %>

## Common commands
```bash
# Development
yarn watch --env entry=<%= entryName %>
yarn watch --env entry=<%= entryName %>,auth,static-pages,login-page,verify,profile

# Mock API
yarn mock-api --responses src/applications/<%= folderName %>/tests/fixtures/mocks/local-mock-responses.js

# Unit tests
yarn test:unit --app-folder <%= folderName %>
yarn test:unit --app-folder <%= folderName %> --log-level all

# E2E tests
yarn cy:open
yarn cy:run --spec "src/applications/<%= folderName %>/tests/<%= entryName %>.cypress.spec.js"
```