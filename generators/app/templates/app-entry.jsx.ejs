import '<%= subFolder %>../../platform/polyfills';
import '<%= subFolder %>../../sass/<%= entryName %>.scss';

import startApp from '<%= subFolder %>../../platform/startup';

import routes from './routes';
import reducer from './reducers';
import manifest from './manifest.json';

startApp({
  url: manifest.rootUrl,
  reducer,
  routes
});
